<!DOCTYPE html>
<html>
<head>
    <title>Outpost</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    {% if stylesheet %}
        <style>{{ stylesheet|raw }}</style>
    {% endif %}
    {% if script %}
        <script>{{ script|raw }}</script>
    {% endif %}
</head>
<body>

    <header>
        {% include "navigation/main.twig" %}
    </header>

    <main>
        {% block body "" %}
    </main>

    <footer>
        <address>This page was generated by Outpost at {{ "now"|date("g:ia") }} on {{ "now"|date("l, F j") }}.</address>
    </footer>

</body>
</html>
